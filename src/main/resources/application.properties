# SPRING CONFIGS
spring.main.allow-bean-definition-overriding=true
# DATABASE
spring.datasource.url=jdbc:mysql://localhost:3306/productsms?useSSL=false&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.generate_statistics=true
# ELASTICSEARCH
elasticsearch.cluster.name=elasticsearch-cluster
elasticsearch.host=localhost
elasticsearch.port=9300
#
# Home directory of the embedded Elasticsearch instance. Default to the
# current working directory.
#
#spring.data.elasticsearch.properties.path.home=target/elastic-embedded
#spring.data.elasticsearch.properties.transport.tcp.connect_timeout=60s